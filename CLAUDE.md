@AGENTS.md

# AI.ZIP — 작업 지시서

> 이 파일은 AI.ZIP 웹사이트를 만들 때 매번 읽는 매뉴얼입니다.
> 배포 주소: https://ai-zip.lhj106147.workers.dev/

---

## 프로젝트 한 줄 요약

**한국인 일반 유저가 필요한 AI 서비스를 가장 빠르게 찾는 미니멀 디렉토리 사이트.**

- 타겟: 한국인 일반 유저 (전문 용어 안 씀, 직관적인 한글 명칭만 씀)
- 성격: 극단적으로 가볍고, 읽기 쉽고, 텍스트/UI가 최소화된 리스트
- 지금 상태: **뼈대만 완성** (헤더 + 사이드바 + 카드 리스트, 데이터는 전부 가짜)

---

## 절대 규칙 (이건 어길 수 없음)

1. **색은 4개만 쓴다.** 흰색 배경 / 검정 글씨 / 회색 보조글씨 / 아주 연한 회색 선. 그 외 색 추가 금지.
2. **위계는 굵기(font-weight)로만 만든다.** 크기·색으로 강조하려 하지 말 것.
3. **대괄호 `[ ]` 금지.** 태그는 둥근 배지 또는 얇은 실선 테두리로.
4. **아이콘은 선(line) 아이콘만.** 속이 꽉 찬 아이콘 금지. 인라인 SVG로 직접 그린다 (아이콘 라이브러리 설치 금지).
5. **텍스트를 줄인다.** 설명은 한 줄. 버튼에 굳이 텍스트 안 넣어도 되면 아이콘만.
6. **API 키·비밀값은 반드시 `.env` 또는 Cloudflare Secret에.** 코드에 직접 쓰지 않는다.
7. **새 라이브러리 설치 전에 물어본다.** 지금 의존성은 next/react/react-dom 3개뿐. 이 상태를 지킨다.

---

## 기술 스택 (설치 완료된 것)

| 항목 | 내용 |
|---|---|
| 프레임워크 | Next.js 16.2.11 (App Router) |
| UI | React 19.2.4 |
| 스타일 | Tailwind CSS v4 (설정 파일 없음, `app/globals.css`에서 직접 설정) |
| 언어 | TypeScript (strict) |
| 배포 | OpenNext → Cloudflare Workers |
| 폰트 | Pretendard (CDN, `app/layout.tsx`의 `<head>`) |

**⚠️ Next.js 16은 예전 버전과 다릅니다.** 코드 쓰기 전에 `node_modules/next/dist/docs/`의 해당 문서를 먼저 읽는다. 기억에 의존해서 예전 방식(`pages/`, `getServerSideProps`, `next/head` 등)으로 쓰지 않는다.

**⚠️ Tailwind v4는 `tailwind.config.js`가 없습니다.** 색·토큰은 `app/globals.css`의 `@theme inline` 블록에서 관리한다.

---

## 파일 구조

```
ai-zip/
├── CLAUDE.md              # 이 파일 (작업 지시서)
├── AGENTS.md              # Next.js 16 주의사항
├── app/
│   ├── layout.tsx         # 전체 뼈대 (Header + Sidebar + main)
│   ├── page.tsx           # 메인 리스트 화면
│   ├── globals.css        # 디자인 토큰 4색 정의
│   └── favicon.ico
├── components/
│   ├── Header.tsx         # 로고 + 캐치프레이즈 + 검색창
│   ├── Sidebar.tsx        # 카테고리 6개 + 라인 아이콘
│   └── AiCard.tsx         # 리스트 한 줄 (로고/이름/태그/요약/↗)
├── public/                # 정적 이미지
├── wrangler.jsonc         # Cloudflare 배포 설정
├── next.config.ts
└── open-next.config.ts
```

건드리면 안 되는 폴더: `.next/`, `.open-next/`, `node_modules/` (전부 자동 생성물)

---

## 디자인 토큰 (색을 쓸 때는 반드시 이걸로)

`app/globals.css`에 정의되어 있습니다.

| 토큰 | 값 | 쓰는 곳 | Tailwind 클래스 |
|---|---|---|---|
| `--background` | `#ffffff` | 배경 | `bg-background` |
| `--foreground` | `#111111` | 본문 글씨 | `text-foreground` |
| `--muted` | `#555555` | 보조 설명, 아이콘 | `text-muted` |
| `--border` | `#f5f5f5` | 선, 배지 배경, hover | `border-border` / `bg-border` |

**`text-gray-500`, `bg-slate-100` 같은 Tailwind 기본 팔레트를 직접 쓰지 않는다.** 위 4개만 쓴다.

---

## 명령어

```bash
npm run dev      # 개발 서버 (http://localhost:3000)
npm run build    # 빌드 확인
npm run lint     # 코드 검사
npm run deploy   # 빌드 + Cloudflare 배포 (실서비스에 바로 반영됨)
```

**`npm run deploy`는 실제 사이트를 바꿉니다. 사장님이 "배포해줘"라고 말하기 전엔 실행하지 않는다.**

---

## 기능 1: 화면 만들기 / 고치기

사장님이 "○○ 화면 만들어줘", "이거 이렇게 바꿔줘"라고 하면:

1. **먼저 계획을 말한다.** 어떤 파일을 만들고/고칠 건지 3줄 이내로. 바로 코드 쓰지 않는다.
2. 승인받으면 작업한다.
3. `components/`에 이미 있는 걸 먼저 찾아본다. 비슷한 게 있으면 새로 만들지 말고 그걸 쓴다.
4. 작업 후 `npm run build`로 깨지지 않았는지 확인한다.
5. 결과는 **화면에서 뭐가 달라졌는지**로 보고한다. 코드 설명은 하지 않는다.

**새 컴포넌트를 만드는 기준:** 두 군데 이상에서 쓰일 때만 만든다. 한 군데서만 쓰면 그 파일 안에 그냥 둔다.

---

## 기능 2: AI 서비스 데이터 추가 / 수정

지금은 `app/page.tsx` 안의 `placeholderItems` 배열이 가짜 데이터입니다.

**진짜 데이터를 넣을 때 각 서비스가 가져야 할 정보:**

| 항목 | 설명 | 예시 |
|---|---|---|
| `name` | 서비스명 | `ChatGPT` |
| `summary` | 한 줄 요약 (25자 내외, 마침표 없이) | `대화로 뭐든 물어보는 만능 AI` |
| `category` | 아래 6개 중 하나 | `문서 및 글쓰기` |
| `priceType` | `무료` / `부분 무료` / `유료` | `부분 무료` |
| `isKorean` | 한국어 지원 여부 | `true` |
| `logoUrl` | 로고 이미지 주소 | |
| `websiteUrl` | 바로가기 링크 | |
| `overview` | 상세페이지에 들어가는 긴 소개글 (문단 3~4개, 빈 줄로 나눔) | 노션 `소개글` 칸에서 관리 |
| `userCount` | 인기 정렬 기준 (숫자) | `100000` |
| `createdAt` | 등록 날짜 ('오늘 추가된 AI' 판별용) | |

**요약 문장 쓰는 규칙:**
- "~하는 AI", "~해주는 도구" 식으로 **뭘 해주는지**만 쓴다
- 기능 나열 금지 ("텍스트 생성, 번역, 요약, 코딩 지원" ❌)
- 영어 전문 용어 금지 ("LLM 기반 멀티모달" ❌ → "그림도 이해하는" ✅)

**카테고리 6개 (이것 외에 추가 금지, `components/Sidebar.tsx`와 항상 일치시킬 것):**
`오늘 새롭게 추가된 AI` / `문서 및 글쓰기` / `학업 및 연구` / `코딩 및 개발` / `이미지 및 영상` / `일상 및 생산성`

**정렬:** 항상 `userCount` 내림차순 (인기순).

---

## 기능 3: 상세 페이지 만들기 (아직 없음)

경로: `app/service/[id]/page.tsx`

구조는 이 3단으로 고정합니다:

```
상단  — 큰 로고 + 서비스명 + 태그 + [↗ 웹사이트 바로가기] 버튼
        (버튼은 굵은 선 박스 형태, 여기서만 텍스트 있는 버튼 허용)
중단  — "이 AI는 어떤 서비스인가요" 긴 소개글 (문단 3~4개)
        · 노션 `소개글` 칸이 우선. 비어 있으면 lib/services.ts의 overviews를 씀
        · 둘 다 비면 이 부분은 통째로 안 보임
      — "이럴 때 쓰면 좋아요" 3~4줄
      — 요약 정보: 회원가입 필요 여부 / 무료 사용 범위 / 한국어 지원
하단  — 관련 추천 AI 2~3개 (AiCard 재사용)
```

---

## 기능 4: 검색

**지금 단계에서는 일반 키워드 검색만 만듭니다.** `Header.tsx`의 검색창이 이미 있고, 기능이 안 붙어 있습니다.

- 서비스명 + 요약문 안에서 글자가 포함되는지만 본다 (그 이상 복잡하게 만들지 않는다)
- 결과 없으면: "찾는 AI가 없어요. 다른 말로 검색해보세요"

---

## 기능 5: 배포

사장님이 "배포해줘"라고 하면:

```bash
npm run build    # 1. 빌드 확인 (에러 있으면 여기서 멈추고 보고)
npm run deploy   # 2. 실제 배포
```

배포 후 보고:
```
배포가 완료되었습니다!
👉 https://ai-zip.lhj106147.workers.dev/

이번에 바뀐 것:
- (달라진 화면을 한 줄씩)

폰에서 한번 확인해보세요. 반영이 안 보이면 새로고침(Ctrl+F5) 해주세요.
```

---

## 보류된 기능 (지금 만들지 않음)

사장님이 요청하지 않는 한 먼저 제안하지 않습니다.

- **AI 대화형 맞춤 검색** — Phase 2. 초기엔 키워드 검색만.
- **데이터베이스(PostgreSQL) 연결** — 서비스 개수가 50개 넘어가면 그때 검토. 그전까진 파일에 목록으로 관리.
- **Google AdSense 광고** — 트래픽 생긴 뒤에.
- **GitHub Actions 매일 자동 업데이트** — 수동으로 몇 번 굴려보고 나서.
- **다크 모드, 로그인, 즐겨찾기, 댓글** — 요청 전까지 만들지 않음.

---

## 남은 일 (MVP 체크리스트)

- [ ] 진짜 AI 서비스 데이터 20~30개 채우기
- [ ] 사이드바 카테고리 클릭 시 실제로 필터링되게 하기
- [ ] 검색창 작동시키기
- [ ] 상세 페이지 만들기
- [ ] 모바일 화면 정리 (지금 사이드바가 폰에서 자리를 너무 차지함)
- [ ] `README.md` 내용 우리 프로젝트에 맞게 교체 (지금은 Next.js 기본 설명문)

---

## 작업 톤

- 사장님한테 보고하듯이 결과물만 전달한다
- "~했습니다", "~만들었습니다" 식으로
- 전문 용어가 꼭 필요하면 괄호 안에 쉬운 설명을 붙인다
- 코드를 설명하지 않는다 — 화면이 어떻게 달라졌는지만 말한다
- 뭘 만들지 애매하면 만들기 전에 물어본다
